<Window x:Class="ScriptMigrationUtility.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScriptMigrationUtility"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <!-- CollectionViewSource for grouping by SourceFile -->
        <CollectionViewSource x:Key="StepsGrouped" Source="{Binding Steps}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="SourceFile"/>
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
    </Window.Resources>
    <Grid>
        <StackPanel Margin="10">
            <DockPanel Margin="5">
                <TextBox Text="{Binding SelectedDirectory}" Width="500" />
                <Button Content="Browse" Command="{Binding BrowseCommand}" Margin="5,0,0,0"/>
            </DockPanel>

            <Button Content="Scan XML Scripts" Command="{Binding ScanCommand}" Margin="5"/>
            <DataGrid ItemsSource="{Binding Source={StaticResource StepsGrouped}}"
          AutoGenerateColumns="False"
          CanUserAddRows="False"
          Height="300"
          Margin="5">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="50">
                        <DataGridTemplateColumn.Header>
                            <TextBlock Text="Select"/>
                        </DataGridTemplateColumn.Header>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="100"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="200"/>
                </DataGrid.Columns>

                <DataGrid.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <TextBlock FontWeight="Bold" FontSize="12" Text="{Binding Name}"/>
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </DataGrid.GroupStyle>
            </DataGrid>


            <Button Content="Convert" Command="{Binding ConvertCommand}" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
